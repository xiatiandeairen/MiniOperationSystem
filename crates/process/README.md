# minios-process — Process Management

`#![no_std]` process control blocks, CPU context, and a fixed-size process table.

## Standalone Usage
```toml
[dependencies]
minios-process = { version = "0.1", default-features = false }
```
