[package]
name = "minios-interrupt"
version = "0.1.0"
edition = "2021"
description = "IDT and interrupt/exception handling for MiniOS"

[dependencies]
minios-common = { path = "../common" }
x86_64 = { workspace = true }
spin = { workspace = true }
