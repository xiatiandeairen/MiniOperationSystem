# minios-ipc — Message Queue IPC

`#![no_std]` inter-process communication via fixed-capacity message queues.

## Standalone Usage
```toml
[dependencies]
minios-ipc = { version = "0.1", default-features = false }
```
