# v0.2.0 Changelog — 全部开发变更

## 新增功能
- `feat(shell): enable process spawn/kill/sched/nice for hands-on scheduling` — 4个进程管理命令 + parse_u32 辅助函数
- `feat(syscall,fs): add trace spans to every syscall and VFS operation` — 7 syscall + 9 VFS 方法全部有 trace span
- `feat(shell): enable trace follow for single-command trace analysis` — trace follow 子命令 + describe_span 教学注解
- `feat(shell): add pagetable/frames/alloc commands for memory exploration` — 3个内存探索命令 + parse_hex 辅助函数

## Bug 修复
- `fix(kernel): remove trace_event from timer ISR to prevent Mutex deadlock` — timer 回调中的 trace_event! 与主线程 trace_span! 竞争 TRACER Mutex

## 依赖变更
- `chore: update Cargo.lock for new trace dependencies` — minios-trace 添加到 syscall 和 fs crate
